# Database connection URL for PostgreSQL running locally in a Docker container
# Format: postgresql://<username>:<password>@<host>:<port>/<database_name>
DATABASE_URL=postgresql://postgres:dev@localhost:5432/tasks
